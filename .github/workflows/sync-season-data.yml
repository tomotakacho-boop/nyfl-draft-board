name: Sync season data

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Pull ESPN and Sleeper data
        env:
          ESPN_1_SWID: ${{ secrets.ESPN_1_SWID }}
          ESPN_1_S2: ${{ secrets.ESPN_1_S2 }}
          ESPN_2_SWID: ${{ secrets.ESPN_2_SWID }}
          ESPN_2_S2: ${{ secrets.ESPN_2_S2 }}
        run: python scripts/sync_leagues.py
      - name: Commit changed snapshot
        run: |
          git config user.name "NYFL data bot"
          git config user.email "actions@users.noreply.github.com"
          git add data/hub.json
          git diff --cached --quiet || git commit -m "Update season data"
          git push
